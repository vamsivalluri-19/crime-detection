// Local fallback for development.
// For Vercel, set the environment variable `API_BASE` in the project settings.
// The build step will inject `window.__API_BASE__` when `API_BASE` is set.
window.__API_BASE__ = 'https://crime-detection-ii5r.onrender.com';